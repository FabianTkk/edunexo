RewriteEngine On
# No redirigir lo que ya va a /edunexo/public/
RewriteCond %{REQUEST_URI} !^/edunexo/public/
# Redirigir todo lo demás a la carpeta public (ruta relativa, sin /)
RewriteRule ^(.*)$ public/$1 [L]
